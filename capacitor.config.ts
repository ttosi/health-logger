import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'health-logger',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
